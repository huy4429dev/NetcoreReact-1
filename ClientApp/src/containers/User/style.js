const styles = (theme)=>({
    btnAdd:{
        marginLeft:'auto',
        marginBottom:'1rem',
        minWidth:'2rem',
        padding:'0.4rem 0.5rem',
        fontSize: '0.8rem',
    }
})

export default styles;